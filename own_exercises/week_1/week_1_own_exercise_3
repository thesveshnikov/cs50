#include <cs50.h>
#include <stdio.h>

int main(void)
{
    float number_1 = get_float("Enter a number: ");
    float number_2 = get_float("Enter another number: ");
    
    if (number_1 > number_2)
    {
        printf("%.2f is greater than %.2f\n", number_1, number_2);
    }
    else if (number_1 < number_2)
    {
        printf("%.2f is greater than %.2f\n", number_2, number_1);
    }
    else
    {
        printf("%.2f and %.2f are the same\n", number_1, number_2);
    }
}
